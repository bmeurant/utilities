#!/bin/bash

# Set the custom Gemini configuration directory
export GEMINI_CONFIG_DIR="$HOME/.gemini_perso"

# Display a message to indicate the CLI is starting in personal mode
echo "Starting Gemini CLI (personal mode)..."

# Launch the Gemini CLI
gemini